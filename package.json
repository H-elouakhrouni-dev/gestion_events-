{
  "name": "gestion-evenements",
  "version": "1.0.0",
  "description": "Plateforme Intelligente de Gestion d'Evenements - Full Stack (Laravel + React)",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"magenta,cyan\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && php artisan serve",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && composer install && cd ../frontend && npm install",
    "setup": "npm run install:all && cd backend && php artisan key:generate && php artisan migrate:fresh",
    "install:frontend": "cd frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
